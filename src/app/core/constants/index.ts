/** enum for filter values */
export enum FilterEnum {
    all = 'all',
    active = 'active',
    completed = 'completed'
}

/** constants related to configs */
export const config = {
    api_url: "https://todo-service-app-default-rtdb.firebaseio.com"
}